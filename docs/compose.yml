services:
  workshop-docs:
    image: nginx:alpine
    container_name: docsify_workshop
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped